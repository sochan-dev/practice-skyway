【Peerのイベント】
open：SkyWayのシグナリングサーバーと接続し（ここまではおそらくPeerをnewしたときに行われる）、準備が整ったときに発火

call：相手から接続要求が来た時に発火。相手との接続を管理するオブジェクトを引数に受け取り、answerメソッドに自分自身のlocalStreamを引数に渡して実行することで映像と音声を送信して応答する
    また、受け取った相手との接続を管理するオブジェクトには相手の映像などのデータも入っている。

error：何らかのエラーが発生したときに発火。エラー内容を引数に受け取る。












